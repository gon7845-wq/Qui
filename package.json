{
  "name": "qui",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:server dev:client",
    "dev:server": "npm run dev -w @qui/server",
    "dev:client": "npm run dev -w @qui/client",
    "build": "npm run build -w @qui/server && npm run build -w @qui/client",
    "typecheck": "npm run typecheck -w @qui/shared && npm run typecheck -w @qui/server && npm run typecheck -w @qui/client"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
